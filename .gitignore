# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Build output
frontend/dist/
*.egg-info/
flight-parser/target/

# Environment / secrets (ADR-0012 — comprehensive)
.env
.env.*
!.env.example
*.env.local
.env.demo.local
.env.bak.*
# private keys / certs
*.pem
*.key
*.p12
*.pfx
*.crt
.secrets/
secrets.yaml
secrets.yml
# misc local secret stores
.netrc
.pgpass
google-credentials.json
service-account*.json

# IDE
.vscode/
.idea/

# Claude Code local settings
.claude/settings.local.json

# Data volumes
/data/

# Local break-glass database dumps (ADR-0041 Procedure D). On BOS-HQ this is
# ~1.1 GB of PLAINTEXT pg dumps holding customer PII, invoice records and
# device_api_keys, living inside the deploy clone's working tree. Untracked is
# not enough — one `git add -A` would commit it. Ignored so it cannot be.
backups/

# OS
.DS_Store
Thumbs.db

# Python
*.pyo
*.pyd
.Python
pip-log.txt

# Docker
docker-compose.override.yml
.claude/worktrees/
# (.env.demo already covered by `.env.*` above — kept here as a comment so
# operators searching for it know the rule lives in the secrets section.)
tools/p-eval/target/
